#include "3-calc.h"

int op_add(int a, int b);
int op_sub(int a, int b);
int op_mul(int a, int b);
int op_div(int a, int b);
int op_mod(int a, int b);

/**
* op_add - returns the sum
* @a: first no
* @b:second no
* Return: the difference
*/
int op_add(int a, int b)
{
	return(a + b);
}

/**
* op_add - returns the difference
* @a: first no
* @b:second no
* Return: the difference
*/
int op_add(int a, int b);
{
        return(a - b)
}

/**
* op_add - returns the product
* @a: first no
* @b:second no
* Return: the product
*/
int op_add(int a, int b)
{
        return(a * b);
}
/**
* op_add - returns the division
* @a: first no
* @b:second no
* Return: the quotient
*/
int op_add(int a, int b);
{
        return(a / b);
}
/**
* op_add - returns the remainder of division
* @a: first no
* @b:second no
* Return: the remainder
*/
int op_add(int a, int b)
{
        return(a % b);
}
