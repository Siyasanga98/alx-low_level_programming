#include "main.h"
#include <unistd.h>
/**
 * _putchar - function writes the character c to stdout
 * @c:c to rint
 * Return: 1 on success, 0n error -1
 */

int _putchar(char c)
{
	return(write(1, &c, 1);
}
